// spacing scale shared across every screen. multiples of 4 keep things tidy.

export const spacing = {
  xxs: 2,
  xs: 4,
  sm: 8,
  md: 12,
  lg: 16,
  xl: 20,
  xxl: 28,
  xxxl: 40,
  screen: 16,
};

export const radii = {
  sm: 6,
  md: 10,
  lg: 14,
  pill: 999,
};

// max width for the web shell so the app reads as a phone in a browser.
export const layout = {
  maxAppWidth: 450,
  bottomTabHeight: 56,
  topBarHeight: 52,
};
